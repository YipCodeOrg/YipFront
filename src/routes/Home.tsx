import {
  VStack,
} from "@chakra-ui/react"
import Hero from "../core/Hero"

export default function Home() {
    return (
      <>
          <VStack> 
          {/*TODO*/} 
            <Hero/>
          </VStack>
      </>
      );
}
